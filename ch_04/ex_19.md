> Created by Erkam on 04/08/25.

# Solution

1. checks if `ptr` is a null pointer and if it's not, it's dereference and increments pointer.
2. checks if `ival` is `1` and increments it, then if it is not `1` checks incremented `ival`.
3. it is undefined behaviour. to be able to compare properly it must be `vec[ival] <= vec[ival+1]`