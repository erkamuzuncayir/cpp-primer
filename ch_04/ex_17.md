> Created by Erkam on 04/08/25.

# Solution

1. With `postfix`, unincremented/decremented value used for suitable operation and increment/decrement happens after expression. But with `prefix`, increment/decrement happens at that moment and expression use this value.