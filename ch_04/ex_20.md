> Created by Erkam on 04/08/25.

# Solution

1. legal. first dereference, then increment.
2. illegal, `*iter` represents a string object, it can't increment.
3. illegal, it should be `iter->empty()`
4. legal.
5. illegal, `*iter` represents a string object, it can't increment
6. legal. first calls empty, then increment.