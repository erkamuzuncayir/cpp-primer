> Created by Erkam on 04/04/25.

# Solution

1. `ip` is `pointer` to `int`. `i` is an `int`. `r` is a reference to `int i`.
2. `i` is an `int`. `ip` is a `null pointer`.
3. `ip` is a `pointer` to `int`, `ip2` is an int.