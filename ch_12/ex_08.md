```
// -----------------------------------------------------------------------------
// File:        ex_08.md
// Author:      Erkam Uzuncayir
// Date:        11/06/2025
// -----------------------------------------------------------------------------
```

# Solution

It should return `int` pointer instead of `bool`. Otherwise `p` implicitly converted to a `bool` therefore, it can't be freed and memory will leak.