> Created by Erkam on 04/16/25.

# Solution

1. Because `s` won't change in function but, `occurs` might change.
2. Because `s` might be too big for an optimized pass by value operation, but `c` is a just a 1 byte char. And `occurs`, to be able to return more information besides return value.
3. It might take too much time.
4. We can't change `occurs` in function.