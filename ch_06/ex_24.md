> Created by Erkam on 04/16/25.

# Solution

Length of array parameter `ia` is unnecessary. Because all parameter arrays pass as a pointer to the first element of array. It limits usefulness of function, so it shouldn't be specified.