> Created by Erkam on 05/05/25.

# Solution

`rem` defined before `base` so, it will came before in terms of initialization order in constructor. This may cause undefined behaviour for initialization of `rem`.
