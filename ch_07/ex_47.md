> Created by Erkam on 06/05/25.

# Solution

It should be explicit. Because `string` is a very commonly used type that it's conversion to `Sales_data` might be error prone.

The drawback is we must use with direct initialization with this constructor.
