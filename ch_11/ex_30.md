```
// -----------------------------------------------------------------------------
// File:        ex_30.md
// Author:      Erkam Uzuncayir
// Date:        06/06/2025
// -----------------------------------------------------------------------------
```

# Solution

`pos` is a pair of iterators that `pos.first` is the first one and `pos.second` is the past-the-end iterator. So, `pos.first->second` is `value` part of the std::pair object that `pos.first` is pointing to. 