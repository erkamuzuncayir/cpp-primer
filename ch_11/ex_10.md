```
// -----------------------------------------------------------------------------
// File:        ex_10.md
// Author:      Erkam Uzuncayir
// Date:        30/05/2025
// -----------------------------------------------------------------------------
```

# Solution

We can define `std::map<std::vector<int>::iterator, int>` because it supports iterator arithmetic which includes necessary `<` operation but, can't define `std::map<std::list<int>::iterator, int>` because it doesn't support iterator arithmetic.