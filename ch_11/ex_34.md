```
// -----------------------------------------------------------------------------
// File:        ex_34.md
// Author:      Erkam Uzuncayir
// Date:        10/06/2025
// -----------------------------------------------------------------------------
```

# Solution

`subscript` operator insert a new element, if element can't found in `map`. but, it can't insert in the `transform` method because, `map` argument is `const` therefore, it won't compile.