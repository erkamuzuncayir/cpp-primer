> Created by Erkam on 09/05/25.

# Solution

1. `++iter` might be outside of `while` block (I don't know if curly braces just skipped?)
2. If `vi` has any odd number, `if` block cause an endless loop.