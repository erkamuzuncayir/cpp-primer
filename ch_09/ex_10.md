> Created by Erkam on 08/05/25.

# Solution

`it1` is `std::vector<int>::iterator`.
`it2` is `std::vector<int>::const_iterator`.
`it3` is `std::vector<int>::const_iterator`.
`it4` is `std::vector<int>::const_iterator`.